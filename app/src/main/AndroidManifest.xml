<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="edu.us.ischool.tanl2.quizdroid">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:name=".QuizApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ActivityOverview">
        </activity>
        <activity android:name=".Preferences">
        </activity>
        <activity android:name=".QuestionMain">
        </activity>
        <activity android:name=".ActivityQA">
        </activity>
        <activity android:name=".AnswerActivity">
        </activity>
        <receiver android:name=".DownloadBroadcastReciever">
            <intent-filter>
                <action android:name="me.proft.alarms.BROADCAST_DOWNLOAD" />
            </intent-filter>
        </receiver>

    </application>

</manifest>